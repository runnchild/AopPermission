// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
    apply from: "https://gitee.com/qiurongc/codes/s3wgd76acpzm8bx5fo49u32/raw?blob_name=config.gradle"
//    apply from: 'config.gradle'
    repositories {
        mavenLocal()
        google()
        jcenter()
    }
    dependencies {
        classpath classpaths
        classpath aspectPlugin, bintrayPlugin
    }
}

allprojects {
    repositories {
        mavenLocal()
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}